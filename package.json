{
  "name": "crowelogic-bio",
  "version": "1.0.0",
  "description": "crowelogic.bio — landing page for Crowe Logic biotech portal (Crowe Psychedelics)",
  "private": true,
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18"
  }
}
